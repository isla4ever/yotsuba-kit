# Today 今日指挥台

> **规划中(P2)**。`<YsToday>` 将提供 widget 注册表(下一节课、课程时间线、计划、天气、上课准备),
> 通过 `createScheduleStore()` 与 `<YsSchedule>` 共享同一学期引擎实例实现联动,
> 支持外部方法动态开关与重排 widget。
>
> API 冻结前欢迎在 GitHub Discussions 提出你的场景需求。
